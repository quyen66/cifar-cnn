# config_detected.txt — dò từ test/logs/ (BƯỚC 0), file đại diện:
# cifar-10_alie_a0.25_s42.log (đối chiếu thêm 2 file khác dataset/attack — khớp)
# Đây là config của LẦN CHẠY TẠO RA CÁC LOG NÀY ("pyproject CŨ") — không phải
# pyproject.toml hiện tại trong repo (có thể đã đổi từ đó tới giờ).

timestamp_ghi: 2026-07-22

## Pipeline / ablation
gas-enabled                 = false   (in "[ABLATION] gas=False ...")
cosine-threshold-mode       = FIXED   (in "cosine_mode=FIXED (warmup_pct=5.0 round_k=2.5)")
alpha-blend / theta-mode    = KHÔNG in ra nữa (đã hard-code — xác nhận gián tiếp: θ_signal
                               cố định 1 giá trị suốt cả run, vd 0.1895 mọi vòng sau warmup
                               → khớp WARMUP_STATIC, không phải GDS_PERROUND)

## Core FL config
num_clients        = 40
attack_ratio        = 0.3   (num_malicious=12, partition_id 0-11; trusted=partition_id 12-35, 24 client)
warmup_rounds       = 10    (ROUND 1-10 = "PHASE 1: TRUSTED WARM-UP", ROUND 11+ = "PHASE 2")
total_rounds        = 50    (suy từ dữ liệu — round cuối parse được, xem integrity check)

## Layer 1 (Magnitude Filter + DBSCAN)
mad_k_reject        = 4.0
mad_k_flag          = 3.0
dbscan_min_samples  = 3
dbscan_eps_mult     = 1.5
pca_dims_target     = 20
zero_cluster_action = accept_all

## Layer 2 (Distance + Direction)
cosine_threshold        = 0.8   (reject threshold, cố định — mode=FIXED)
distance_multiplier     = 2.5
enable_rescue           = True
rescue_cosine_threshold = 0.9
require_distance_pass   = True

## GDS (NonIIDHandler)
GDS_weights          = CV=0.4, sim=0.4, cluster=0.2
normalization         = CÓ BẬT (log in cả "_raw" và "_norm" cho D_CV/D_sim/D_cluster,
                         vd "D_CV_raw=0.173 D_CV_norm=0.067×0.4" — xác nhận
                         GDS_NORM_RANGES_CIFAR10/_normalize_gds_component vẫn hoạt động,
                         KHÔNG bị gỡ như dự định ở "Final cleanup project" Giai đoạn 2a)
θ_adj formula         = clip(θbase + (0.5 - θ_signal) × 0.2, 0.5, 0.8)   (adjustment_factor=0.2)
θ_signal (in log)     = CỐ ĐỊNH 1 giá trị suốt post-warmup (vd 0.1895) → xác nhận
                         theta-signal-mode=WARMUP_STATIC bị khóa cứng (không còn switch)
⚠️ θ_adj THỰC TẾ (giá trị ngưỡng hard/soft sau công thức) KHÔNG được in ra log —
   chỉ in θ_signal (input). round_meta.csv cột "theta_adj" ghi θ_signal (input),
   KHÔNG phải ngưỡng hard/soft đã tính — ghi rõ để tránh hiểu nhầm.

## Behavioral / Confidence / TwoStageFilter
H_weights            = grad=0.4, loss=0.3, acc=0.3
confidence_weights   = flag=0.3, euclidean=0.2, baseline=0.5   (weight_baseline=0.5, khớp GOLDEN)
hard_threshold_base  = 0.7
soft_threshold_base  = 0.06
R_floor              = 0.3

## GIỚI HẠN DỮ LIỆU LOG (báo rõ, không tự suy)
- l1_norm (giá trị norm gradient THÔ của TỪNG client) KHÔNG được in ở đâu trong log —
  chỉ có median_norm/MAD/threshold (mức ROUND, chung cho cả round) và trạng thái
  REJECTED/FLAGGED/ACCEPTED per-client (từ cột "L1 Status" trong bảng Layer 2
  Per-Client Metrics — bảng NÀY đầy đủ mọi client, không bị "..." cắt bớt như
  danh sách ACCEPTED trong khối "LAYER 1 RESULTS").
  → round_detail.csv: cột l1_norm để TRỐNG, có l1_median/l1_mad/l1_thr_reject/
    l1_thr_flag (mức round) + l1_status (mức client, từ bảng L2).
- l2_thr_cosine = hằng số 0.8 (mode FIXED) mọi vòng — không có "trôi" (đúng vì đang
  ở G_OFF/cosine_mode=FIXED cho toàn bộ ma trận test/logs này).

## Regex dùng để parse (xem run_l1l2_deep_analysis.py)
FILENAME_RE        = ^(cifar-10|fashion-mnist)_(.+)_a([\d.]+)_s(\d+)\.log$
ROUND_PHASE_RE      = ROUND (\d+) - PHASE
L1_HEADER_RE        = LAYER 1 RESULTS - Round (\d+)
L1_MEDIAN_RE        = Median norm:\s*([\d.]+)
L1_MAD_RE           = MAD:\s*([\d.]+)
L1_THR_REJECT_RE    = Threshold REJECT \(k=[\d.]+\):\s*([\d.]+)
L1_THR_FLAG_RE      = Threshold FLAG \(k=[\d.]+\):\s*([\d.]+)
L2_TABLE_HEADER_RE  = Layer 2 Per-Client Metrics - Round (\d+)
L2_DIST_THR_RE      = Distance threshold:\s*([\d.]+)
L2_COS_THR_RE       = Cosine reject threshold:\s*([\d.]+)
L2_SUMMARY_HEADER_RE = Layer 2 Summary - Round (\d+)
GT_RE               = Ground Truth from Metrics:\s*(\d+) attackers in batch
ROUND_METRICS_RE    = 📊 METRICS ROUND (\d+)
ACC_RE              = \[AGGREGATED\] Accuracy:\s*([\d.]+)
VARIANT_B_RE        = \[VARIANT B\] GDS=([\d.]+) \| H=([\d.]+) \| θ_adj signal=([\d.]+)
Table row (per-client, 8 cột phân cách '|'): ID | L1 Status | Cosine | Distance |
  Dist OK | Cos OK | Rescue? | GT  — parse bằng split("|"), cột 0 phải isdigit().

## Kết luận BƯỚC 0
Format khớp hoàn toàn với các lần dò trước (project threshold-portability +
cosine-threshold-compare + test-matrix reparse) — KHÔNG lệch. Tự tin chạy
BƯỚC 1 (parse full).
